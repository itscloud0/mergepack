# Config Fixtures

Small repositories used to verify mergepack config behavior.

- `python-repo` uses `.mergepack.json` to prefer nox and mark `checks/*.py` as tests.
- `node-repo` uses `mergepack.json` to prefer pnpm and mark `apps/*/e2e/*.ts` as tests.
